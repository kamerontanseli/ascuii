import { describe, it, expect } from 'vitest';

describe('math', () => {
  it('adds numbers', () => {
    expect(1 + 2).toBe(3);
  });

  it('multiplies numbers', () => {
    expect(2 * 3).toBe(6);
  });
});
